rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for local development and shared testing as per user request
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
